general {
    no_fade_in = false
    grace = 0
    disable_loading = true
    numlock_by_default = true 
}

background {
    monitor =
    path = screenshot
    blur_passes = 2 
    blur_size = 2
    noise = 0.0117
    contrast = 0.8916
    brightness = 0.65
    vibrancy = 0.2
}

# 1. HORA (Formato 12 horas con AM/PM)
label {
    monitor =
    # Usamos el comando date para dar formato de 12 horas (%I:%M %p)
    text = cmd[update:1000] echo "$(date +'%I:%M:%S %p')"
    color = rgb(62, 155, 148) 
    font_size = 90
    font_family = JetBrainsMono Nerd Font ExtraBold
    position = 0, 150
    halign = center
    valign = center
}

# 2. FECHA COMPLETA (Nueva info)
label {
    monitor =
    # Muestra el día de la semana, día del mes y el año
    text = cmd[update:43200000] echo "$(date +'%A, %d de %B')"
    color = rgb(167, 224, 219)
    font_size = 22
    font_family = JetBrainsMono Nerd Font Bold
    position = 0, 70
    halign = center
    valign = center
}

# Saludo Inteligente
label {
    monitor =
    # Un script de una sola línea que revisa la hora actual para decidir el saludo
    text = cmd[update:60000] H=$(date +%H); if [ $H -lt 12 ]; then echo "Buenos Dias, srluis2080 "; elif [ $H -lt 18 ]; then echo "Buenas Tardes, srluis2080 "; else echo "Buenas Noches, srluis2080 "; fi
    color = rgb(167, 224, 219)
    font_size = 18
    font_family = JetBrainsMono Nerd Font Bold
    position = 0, 80
    halign = center
    valign = bottom
}


# INPUT FIELD (Caja de contraseña)
input-field {
    monitor =
    size = 280, 50
    outline_thickness = 4
    dots_size = 0.25
    dots_spacing = 0.3
    dots_center = true
    
    outer_color = rgb(30, 47, 185) rgb(54, 220, 148) 45deg
    inner_color = rgba(26, 27, 38, 0.75)
    font_color = rgba(200, 200, 200, 1.0)
    active_color = rgba(255, 255, 255, 1.0)
    backspace_color = rgb(128, 155, 218)

    fail_text = <i>Contraseña incorrecta ($ATTEMPTS)</i> # Texto de error personalizado
    fail_color = rgb(128, 155, 218) # El borde cambia de color si fallas
    
    check_color = rgba(100, 235, 237, 1.0) # El borde gira en color cian mientras procesa
    ,
    fade_on_empty = false
    placeholder_text = <span foreground="##64ebed"><i>Introduce la contraseña...</i></span>
    hide_input = false
    capslock_color = rgb(95, 176, 201) # Se resalta el borde si el capslock esta activo
    
    position = 0, -40
    halign = center
    valign = center
}

# Contenedor Píldora Disco
input-field {
    monitor =
    size = 180, 35
    capslock_color = rgb(95, 176, 201)
    fail_color = rgb(128, 155, 218)
    outline_thickness = 3
    inner_color = rgba(26, 27, 38, 0.85)
    fade_on_empty = false
    placeholder_text =     # Vacío para que sirva de fondo
    hide_input = true
    position = 30, 85
    outer_color = rgb(30, 47, 185) rgb(54, 220, 148) 45deg
    halign = left
    valign = bottom
}

# Texto del Disco
label {
    monitor =
    # Ejecuta df y extrae el espacio usado y total de la raíz (/)
    text = cmd[update:60000] echo "󰋊   $(df -h / | awk 'NR==2 {print $3 "/" $2}')"
    color = rgba(255, 255, 255, 0.9)
    font_size = 11
    font_family = JetBrainsMono Nerd Font Bold
    position = 45, 93
    halign = left
    valign = bottom
}

# Contenedor Píldora RAM
input-field {
    monitor =
    size = 180, 35
    capslock_color = rgb(95, 176, 201)
    fail_color = rgb(128, 155, 218)
    outline_thickness = 3
    outer_color = rgb(30, 47, 185) rgb(54, 220, 148) 45deg
    inner_color = rgba(26, 27, 38, 0.85)
    fade_on_empty = false
    placeholder_text = 
    hide_input = true
    position = 30, 40
    halign = left
    valign = bottom
}

# Texto de la RAM
label {
    monitor =
    # Ejecuta free -h y acomoda el consumo actual vs el total
    text = cmd[update:5000] echo "   $(free -h | awk 'NR==2 {print $3 "/" $2}' | sed 's/Gi/G/g')"
    color = rgba(255, 255, 255, 0.9)
    font_size = 11
    font_family = JetBrainsMono Nerd Font Bold
    position = 45, 48
    halign = left
    valign = bottom
}

# Contenedor Píldora Uptime
input-field {
    monitor =
    size = 250, 35
    capslock_color = rgb(95, 176, 201)
    fail_color = rgb(128, 155, 218)
    outline_thickness = 3
    outer_color = rgb(30, 47, 185) rgb(54, 220, 148) 45deg
    inner_color = rgba(26, 27, 38, 0.85)
    fade_on_empty = false
    placeholder_text = 
    hide_input = true
    position = 30, -30
    halign = left
    valign = top
}

# Texto del Uptime
label {
    monitor =
    # Usa el comando uptime -p simplificado (ej: "up 2 hours, 5 minutes")
    text = cmd[update:60000] echo "󱎫 $(uptime -p | sed 's/up //') "
    color = rgba(255, 255, 255, 0.9)
    font_size = 10
    font_family = JetBrainsMono Nerd Font Bold
    position = 42, -39
    halign = left
    valign = top
}

# Contenedor Píldora Actualizaciones
input-field {
    monitor =
    size = 170, 35
    capslock_color = rgb(95, 176, 201)
    fail_color = rgb(128, 155, 218)
    outline_thickness = 3
    outer_color = rgb(30, 47, 185) rgb(54, 220, 148) 45deg
    inner_color = rgba(26, 27, 38, 0.85)
    fade_on_empty = false
    hide_input = true
    position = 30, 130   # Encima de la RAM y el Disco
    halign = left
    valign = bottom
}

# Texto de Actualizaciones
label {
    monitor =
    # Cuenta las líneas de checkupdates (actualizaciones listas)
    text = cmd[update:3600000] echo "󰏔   $(checkupdates | wc -l) Packages"
    color = rgba(255, 255, 255, 0.9)
    font_size = 11
    font_family = JetBrainsMono Nerd Font Bold
    position = 45, 138
    halign = left
    valign = bottom
}

# Contenedor Píldora Procesos
input-field {
    monitor =
    size = 125, 35
    capslock_color = rgb(95, 176, 201)
    fail_color = rgb(128, 155, 218)
    outline_thickness = 3
    outer_color = rgb(30, 47, 185) rgb(54, 220, 148) 45deg
    inner_color = rgba(26, 27, 38, 0.85)
    fade_on_empty = false
    hide_input = true
    position = -180, -30   # Se coloca justo a la izquierda del Bluetooth
    halign = right
    valign = top
}

# Texto de Procesos
label {
    monitor =
    # Cuenta los procesos activos en el sistema
    text = cmd[update:5000] echo " $(ps ax | wc -l) Tareas"
    color = rgba(255, 255, 255, 0.9)
    font_size = 10
    font_family = JetBrainsMono Nerd Font Bold
    position = -192, -39
    halign = right
    valign = top
}

# 3. NOMBRE Y BATERÍA DEL HEADSET BLUETOOTH (Letras Blancas)
label {
    monitor =
    # Busca el dispositivo conectado, extrae su nombre comercial y su porcentaje de batería
    text = cmd[update:10000] MAC=$(bluetoothctl devices Connected | awk '{print $2}' | head -n 1); if [ -n "$MAC" ]; then NAME=$(bluetoothctl info $MAC | grep "Name:" | cut -d' ' -f2-); PCT=$(upower -i $(upower -e | grep 'headset' | head -n 1) | grep percentage | awk '{print $2}'); echo " $NAME ($PCT)"; else echo " Sin dispositivo"; fi
    color = rgba(255, 255, 255, 0.9) # Blanco puro con una pizca de transparencia estética
    font_size = 11
    font_family = JetBrainsMono Nerd Font Bold
    position = -10, 10
    halign = right
    valign = bottom
}
